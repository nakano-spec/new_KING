<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="utf-8">
    <title>ユーザー情報：変更</title>
    <link type="text/css" rel="stylesheet" href="/stylesheets/account_additionChange.css">
</head>

<body>
    <h1>回答即時判断システム：ユーザー情報の変更</h1>
    <hr>
    <h2>ユーザー情報</h2>
    <h3>変更したい項目を変更してください。</h3>
    <div id="data"></div>
    <div id="form">
        <div id="text">
            ユーザー名：<input type="text" id="nameText" value="<%= user_name %>">
            <p></p>
            <br>学籍番号　 : <input type="text" id="studentNumberText" value="<%= user_ID %>">
            <p></p>
            <br>パスワード : <input type="text" id="passwordText" value="<%= password %>">
            <p></p>
            <br>確認　　　 : <input type="text" id="confirmationText">
            <p></p>
            <br>教師権限 : <input type="checkbox" id="authority">
        </div>
        <P></P>
        <p></p>
        <div id="button">
            <input type="button" value="戻る" id="submit" onclick="location.href='account'">
            <input type="button" value="変更" id="submit" onclick="updateUserData()">
        </div>
    </div>

    <script>
        // URLからクエリパラメータを取得する関数
        /*
        function getQueryParam(name) {
            const urlParams = new URLSearchParams(window.location.search);
            return urlParams.get(name);
        }

        // ユーザー情報を取得してフォームにセットする関数
        function setUserData() {
            const userName = getQueryParam('user_name');
            const userID = getQueryParam('user_ID');
            const password = getQueryParam('password');

            document.getElementById('nameText').value = userName;
            document.getElementById('studentNumberText').value = userID;
            document.getElementById('passwordText').value = password;
            // 他の項目も同様にセットすることができます
        }

        // ページが読み込まれたときにユーザー情報をセットする
        window.onload = setUserData;

        // ユーザー情報を更新する関数（この部分は実際の更新処理に置き換えてください）
        function updateUserData() {
            const userName = document.getElementById('nameText').value;
            const userID = document.getElementById('studentNumberText').value;
            const password = document.getElementById('passwordText').value;
            // 他の項目も同様に取得できます

            // ここで取得した情報をデータベースなどに送信して更新処理を行う
            // この部分はサーバーサイドの処理が必要です
        }*/
    </script>

</body>

</html>